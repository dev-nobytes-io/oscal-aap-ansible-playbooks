"""Generators. Everything here writes files that must never be hand-edited.

CI regenerates and fails on a diff, which is what keeps the OSCAL coverage
claims from drifting away from the code that produces them.
"""
